import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "Inter Variable, Noto Sans Thai Looped",
    button: {
      fontWeight: "bold",
      color: "blue",
    },
  },
});
